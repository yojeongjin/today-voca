import Lottie from 'lottie-react';
import neutral from '../../../../public/json/neutral.json';

const Neutral = () => {
  return <Lottie animationData={neutral} loop={true} />;
};

export default Neutral;
