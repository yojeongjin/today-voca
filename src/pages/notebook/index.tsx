import { NextPage } from 'next';
import NotebookContainer from '@/container/Notebook/NotebookContainer';

const Notebook: NextPage = () => {
  return <NotebookContainer />;
};

export default Notebook;
